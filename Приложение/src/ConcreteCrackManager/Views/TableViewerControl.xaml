
<UserControl x:Class="ConcreteCrackManager.Views.TableViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="4">
            <TextBlock Text="{Binding Title}" FontWeight="Bold" VerticalAlignment="Center" Margin="4"/>
            <Button Content="Add" Click="OnAdd" Margin="4"/>
            <Button Content="Edit" Click="OnEdit" Margin="4"/>
            <Button Content="Delete" Click="OnDelete" Margin="4"/>
            <Button Content="Refresh" Click="OnRefresh" Margin="4"/>
        </StackPanel>

        <DataGrid x:Name="Grid" AutoGenerateColumns="True" IsReadOnly="True"/>
    </DockPanel>
</UserControl>
